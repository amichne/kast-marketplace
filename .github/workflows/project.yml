name: Project adaptable marketplace

on:
  pull_request:
  push:
    branches: [main]
  release:
    types: [published]

permissions:
  contents: read

jobs:
  project:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        harness: [codex, github-copilot]
    steps:
      - name: Check out source
        uses: actions/checkout@v5

      - name: Project source
        id: projection
        uses: amichne/intelligence@85625153d6ab7efbfed8a23558749ab16393cd7d # v0.2.9
        with:
          source: .
          harness: ${{ matrix.harness }}
          version: v0.2.9

      - name: Upload projection
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.harness }}-projection
          path: ${{ steps.projection.outputs.projection-path }}
          include-hidden-files: true
